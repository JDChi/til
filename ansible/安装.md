# 安装
## Ubuntu
```shell
$ sudo apt update
$ sudo apt install software-properties-common   # 安装 python 环境
$ sudo add-apt-repository --yes --update ppa:ansible/ansible # 添加源，ansible 在 ppa 上
$ sudo apt install ansible # 安装 ansible
```